import HeroContent from "./HeroContent";

function HeroContainer() {
  return (
    <section className="bg-bg-primary h-155 max-[650px]:h-full">
      <HeroContent />
    </section>
  );
}

export default HeroContainer;
// max-[1100px]:h-[90vh] max-[950px]:h-[82vh]
